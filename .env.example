# OpenAI API Key (Required)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI Model (Optional - defaults to gpt-4.1-mini)
OPENAI_MODEL=gpt-4.1-mini

# Anomaly Detection Threshold (Optional - defaults to 3.0)
# Higher values = fewer anomalies detected, lower values = more anomalies
ANOMALY_THRESHOLD=3.0

# Database Connections
# For local development (when running databases in Docker but agent locally)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=defra_agent
PG_DSN=dbname=defra user=defra password=defra host=localhost

# API Endpoints (Optional overrides)
# PUBLIC_REGISTERS_DIST_KM=3
